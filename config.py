# Global flag to control logging
log_values = True  # Set it to True to enable logging, or False to disable
log_to_file = True
progress_bar = True
log_directory = "logs"
log_file = log_directory + "/mp_db"
id_file = log_directory + "/unique_id.json"